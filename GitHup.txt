
git config --global user.name "Andrés66666"
git config --global user.email "cya2024773@est.univalle.edu"

git init
git status
git add .
git commit -m "fin"
git remote add origin https://github.com/Andres66666/FrontendEcuacion.git
git push -u origin Produccion

git push -u origin main
git checkout -b Produccion 


git remote remove origin
git remote add origin https://github.com/Andres66666/backendEcuacion.git
git remote -v

git config --global user.name "Andrés66666"
git config --global user.email "cya2024773@est.univalle.edu"    

git init
git status
git add .
git commit -m "fin"
git remote add origin https://github.com/Andres66666/backendEcuacion.git
git push -u origin Produccion

git push -u origin main
git checkout -b Produccion 









git config --global user.name "Andrés66666"
git config --global user.email "cya2024773@est.univalle.edu"

git init
git status
git add .
git commit -m "Primer lib"
git remote add origin https://github.com/Andres66666/GuardianUnivalle-Benito-Yucra.git
git push -u origin main
git checkout -b Produccion 
git push -u origin Produccion


git pull origin master
git fetch --all
git reset --hard origin/master


""" DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "Ecuacion",
        "USER": "postgres",
        "PASSWORD": "13247291",
        "HOST": "localhost",
        "PORT": "5432",
    }
} """

pip install -r requirements.txt

gunicorn main.wsgi


python manage.py runserver 0.0.0.0:8000